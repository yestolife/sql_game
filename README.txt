  
Web在线运行：
直接访问：https://yestolife.github.io/sql_game/
推建使用Chrome浏览器访问，第一次点RUN执行SQL时需要加载数据，可能较慢。


本地部署方法：
1.在index.html目录下打开命令行，使用python3 -m http.server启动一个简单的web服务。

2.在谷歌浏览器中，输入127.0.0.8000访问index.html。
点击RUN可执行SQL，第一次点RUN执行SQL时需要加载数据，可能较慢。
